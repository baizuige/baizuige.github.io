[{"title":"作用域与闭包","url":"https://baizuige.github.io/2021/01/25/作用域和闭包/","content":"尝试使用费曼学习法，所以不会说一些专业术语，都是我自己对作用域和闭包的理解，更专业的解释可以看《你不知道的JavaScript（上卷）》\n一个一个房间，大的房间中可以包含小的房间，但有一个最大的房间，也就是全局作用域。\n小房间的人们如果想看书，可以先在自己的房间中找找，如果找不到也可以去大房间中找，直到最大的那个房间，但要注意，不能跑到大房间中其他人的小房间找。 \n如果有一本书，最大的房间也有，自己顺着上面说的那条路找上去也可以找到另外一本，这时虽然说可以不用到最大的房间找，但如果就是想看那本，就可以通过window.var直接从最大的房间中找，也只有最大房间中的书可以这样找。\n找到书后可以在书中随便做笔记了，但之后要立刻还回远处，供别人使用，但书的质量就很难保证了。\n对于用不同方式编写的书，会有不同的效果，var型书籍除非在函数造的房间中编写，否则都会自动算作是最大房间的书；let和const型书籍在编写后就只属于自己小房间了。\n当一个小房子中不再想看书，不再有什么活动时，就会被拆除，包括房间中的所有东西。当然，小房间里的活动也算大房间的。 要注意的是，函数造的房间一般不会被拆除，因为有钥匙的人就可以进去，除非之后真的没有人想到这房间里了。\n现在可以停下来想想，上面的这些机制会有什么奇妙的效果！\n由于进入函数房间只需要有钥匙就可以，那么当配一把钥匙拿到外面的时候。外面的人就可以通过钥匙直接进到函数房间，而在函数房间就可以自由的去大房间找书，这就是闭包。\n","categories":["笔记"],"tags":["JavaScript","闭包"]},{"title":"JavaScript基础知识","url":"https://baizuige.github.io/2021/01/23/JavaScript基础知识/","content":"&emsp;写这篇文档的原因是我又发现了一个宝藏，后盾人的向军，大叔的几个视频，帮助我很快的理解了Symbol、Map、Set，也因为大叔那优雅的代码风格，于是我准备再次重新学习JS，并记下笔记，防止以后又重新学。\n第一章 走进JavaScript\n在直接使用变量name的时候出现问题要知道可能是跟窗体有关\n变量提升：对于var声明的变量会在解析的时候提升到代码前面，但不赋值；let const会进行解析，但有临时性死区，更合理。\n虽然在声明变量的时候可以不写关键字，但这样会污染全局，可以使用严格模式避免\n过去使用立即执行函数来控制var的作用域。\nvar声明的变量会放到window对象中。\nObject.freeze(obj)冻结对象中的属性，在严格模式下修改属性会报错\n传值和传址    值类型和引用类型\n写JS前先加上&quot;use strict&quot;,规范自己的代码，也防止被糟糕的代码恶心\n\n第二章 运算符与流程控制ps：绝大多少语法都是和C/C++相通的\n\n==会进行形式转换，值相等即可；===为绝对等于，包含类型检测。\n\n或运算符可以用于赋值。\nlet a = 3, b = 0;\nlet c = b || a; //c=3;\nswitch中的case可以使用表达式。\nlet a = 3;\nswitch(true)&#123;\n    case a>1:\n        console.log(\"a>1\");\n        break;\n    case a&lt;1:\n        console.log(\"a&lt;1\");\n        break;\n&#125; //a>1\n子层循环想退出父层循环可以使用 break:标签，类似于goto，但不建议使用。\n\nfor-in取的是索引或键名，for-of取的是值。\n\n\n第三章 值类型使用\ntypeof打印变量类型，数组和对象都是Object，未声明和未赋值都是undefined。\n\n可以使用instanceof判断是否为数组  //(arr instanceof Array)。\n\n标签模板，strings表示模板字面量中的字符串，lets表示模板字面量中的变量\nfunction links(strings, ...lets)&#123;\n    console.log(strings); //[\"name:\", \",age:\", \".\", raw: Array(3)]\n    console.log(lets); //[\"baizuige\", 21]\n&#125;\nlinks`name:$&#123;user.name&#125;,age:$&#123;user.age&#125;.`; //调用函数\n字符串常用属性 length、函数toUpperCase() toLowerCase() trim()\n\n字符串截取 slice() substring substr\n\nslice(n[,m])截取从n到m的字符，不包含m，支持负数\nsubstring(n[,m])截取从n到m的字符，不包含m，负数记为0\nsubstr(n[,m])截取从n开始的m个字符，n支持负数，m负数记为0\n\n\n字符串检索 indexOf() includes() lastIndexOf() startsWith() endsWith()\n\nindexOf(str[,n])从n位置开始向后查找str，找到则返回第一个字符的下标，没有则返回-1\nincludes(str[,n])从n位置开始向后查找str,找到则返回true，没有则返回false\n\n\n字符串替换 replace(&quot;old&quot;,&quot;new&quot;)\n\n字符串重复 repeat(n) //重复n次\n\n类型转换  ps：不全\n\n 字符串转为数值 Number(str)、+str\n 数值转为字符串 String(num)、num+&quot;&quot;\n 转为布尔 !!let、Boolean(let)\n parseInt()取字符串前面的数值\n split(&quot;&quot;)字符串拆分为数组\n\n\n数值类型除了0都是真，字符串类型除了空字符串都为真、数组、对象在使用Boolean()作类型转换时总是为真\n\ntoFixed(n)浮点数四舍五入并保留n位小数\n\nMath中常用函数min()、max()、ceil()、floor()、round()\n\nMath.random()取随机数、取值范围[0,1)\n\n日期类型 new Date()\n\n年getFullYear()、月getMonth()、日getDate()、星期getDay()、时getHours()、分getMinutes()、秒getSeconds()\n获取时间戳date.valueOf()和+date\n\nconsole.time(\"for\");\nfor(let i = 0; i &lt; 1000; i++)&#123;&#125;\nconsole.timeEnd(\"for\"); //for: 0.048828125 ms\n日期处理库 Moment.js\n\n\n第四章 数组ps：引用类型，可以存储对象\n\nnew Array(6)创建6个空元素\n\nArray.from(str)就可迭代对象或类数组对象转为数组 //ps:不完整\n\n[...arr]展开数组或收集\n\n解构语法\nlet arr = [\"baizuige\", 21];\nlet [name,age] = arr; //name = \"baizuige\"; age = 21;\nlet a = 2, b = 3;\n[b, a] = [a, b]; //a = 3; b = 2;\n\n数组方法备忘单：\n\n添加/删除元素：\npush(...items) —— 向尾端添加元素，\npop() —— 从尾端提取一个元素，\nshift() —— 从首端提取一个元素，\nunshift(...items) —— 向首端添加元素，\nsplice(pos, deleteCount, ...items) —— 从 pos 开始删除 deleteCount 个元素，并插入 items。\nslice(start, end) —— 创建一个新数组，将从索引 start 到索引 end（但不包括 end）的元素复制进去。\nconcat(...items) —— 返回一个新数组：复制当前数组的所有元素，并向其中添加 items。如果 items 中的任意一项是一个数组，那么就取其元素。\n\n\n搜索元素：\nindexOf/lastIndexOf(item, pos) —— 从索引 pos 开始搜索 item，搜索到则返回该项的索引，否则返回 -1。\nincludes(value) —— 如果数组有 value，则返回 true，否则返回 false。\nfind/filter(func) —— 通过 func 过滤元素，返回使 func 返回 true 的第一个值/所有值。\nfindIndex 和 find 类似，但返回索引而不是值。\n\n\n遍历元素：\nforEach(func) —— 对每个元素都调用 func，不返回任何内容。\n\n\n转换数组：\nmap(func) —— 根据对每个元素调用 func 的结果创建一个新数组。\nsort(func) —— 对数组进行原位（in-place）排序，然后返回它。\nreverse() —— 原位（in-place）反转数组，然后返回它。\nsplit/join —— 将字符串转换为数组并返回。\nreduce/reduceRight(func, initial) —— 通过对每个元素调用 func 计算数组上的单个值，并在调用之间传递中间结果。\n\n\n其他：\nArray.isArray(arr) 检查 arr 是否是一个数组。\n\n\n\n请注意，sort，reverse 和 splice 方法修改的是数组本身。\n这些是最常用的方法，它们覆盖 99％ 的用例。但是还有其他几个：\n\narr.some(fn)/arr.every(fn) 检查数组。\n与 map 类似，对数组的每个元素调用函数 fn。如果任何/所有结果为 true，则返回 true，否则返回 false。\n这两个方法的行为类似于 || 和 &amp;&amp; 运算符：如果 fn 返回一个真值，arr.some() 立即返回 true 并停止迭代其余数组项；如果 fn 返回一个假值，arr.every() 立即返回 false 并停止对其余数组项的迭代。\n我们可以使用 every 来比较数组：\nfunction arraysEqual(arr1, arr2) &#123;\n  return arr1.length === arr2.length &amp;&amp; arr1.every((value, index) => value === arr2[index]);\n&#125;\n\nalert( arraysEqual([1, 2], [1, 2])); // true\n\narr.fill(value, start, end)—— 从索引 start 到 end，用重复的 value 填充数组。\n\narr.copyWithin(target, start, end) —— 将从位置 start 到 end 的所有元素复制到 自身 的 target 位置（覆盖现有元素）。\n\narr.flat(depth)/arr.flatMap(fn) 从多维数组创建一个新的扁平数组。\n\narr.of(element0[, element1[, …[, elementN]]]) 基于可变数量的参数创建一个新的 Array 实例，而不需要考虑参数的数量或类型。\n\n\n\n\n\n第五章 Symbol、Set和MapSymbol类型\nSymbol()创建一个Symbol类型\nSymbol.for()在全局符合注册表中创建并重用符号\nSymbol.keyFor()查询全局注册表\nObject.getOwnPropertySymbols()遍历Symbol属性\nReflect.ownKeys()遍历所有属性\n\nSet和WeakSet类型ps：去重的数组\n\n通过new声明Set类型时可以在（）中包含要存入的元素\n\n\nadd()向Set类型变量添加元素、size获取元素个数、has()判断元素是否存在、delete()删除元素,没有则返回false、values()获取变量中的元素、clear()删除变量中的所有元素\n\nlet set = new Set(\"baizuige\"); //Set(7) &#123;\"b\", \"a\", \"i\", \"z\", \"u\", …&#125;\nlet set = new Set([\"bai\",\"zui\",\"ge\"]); //Set(3) &#123;\"bai\", \"zui\", \"ge\"&#125;\nset.add(1);\nset.size;\nset.has(\"bai\"); //ture\nset.delete(\"zui\");\nset.values();\nset.clear();\n\n由于第一个特性可以简单的将一个字符串拆分为数组，并执行一些操作\n\nlet a = \"123456789\";\nlet set = new Set(a);\nlet arr = [...set].filter(function(item)&#123;\n\treturn item &lt; 5;\n&#125;);\nconsole.log(arr); //(4) [\"1\", \"2\", \"3\", \"4\"]\n\n由于Set中会消除重复的元素，所有可以用来去重。\n\nlet a = [1,2,3,4,5,4,3,2,1];\nconsole.log(new Set(a)); //Set(5) &#123;1, 2, 3, 4, 5&#125;\nconsole.log([...new Set(a)]); //(5) [1, 2, 3, 4, 5]\n\nWeakSet中存放的是引用类型，且为弱引用，不能进行遍历，当引用的元素删除后不会保存引用。 ps：不了解使用场景，大概是为了不浪费资源。\n\nMap和WeakMap类型ps：更强大的对象\n\nMap可以将任何类型作为键名，不像对象只能使用字符串作键名\n\n\nset(,)像Map类型变量添加元素、size获取元素个数、has()判断元素是否存在、delete()删除元素,没有则返回false、keys()获得变量中元素的键、 values()获取变量中元素的值、entries()获得变量中元素的键值对、clear()删除变量中的所有元素\n\nlet map = new Map([[\"name\",\"baizuige\"],[\"age\",\"21\"]]); //Map(2) &#123;\"name\" => \"baizuige\", \"age\" => \"21\"&#125;\nlet map = new Map();\nmap.set(\"name\",\"baizuige\");\nmap.set(function()&#123;&#125;,\"function()\");\nmap.set(&#123;&#125;,\"array\");\nmap.set(1,\"number\");\n\n由于Map的键名可以是对象，那么可以用Map来存储DOM对象\n\n\nWeakMap类型的键必须是对象，同时也是弱引用类型，不能进行遍历\n\n第六章 函数\n函数声明会产生函数声明提升，即可以在函数声明之前调用函数；同时会保存在window对象中\n\n函数表达式不能在函数声明前调用函数，也不会保存在window对象中\n\n立即执行函数\n(function()&#123;\n\tconsole.log(\"Hello World!\");\n&#125;)();\n​    arguments保存函数的所有参数，类数组，老语法，现在可以直接使用点语法\n\n作为参数传递跟另一个函数的函数叫回调函数\n\n函数中的this是window对象，方法中的this是当前对象；方法中可以将this赋值给变量让其内部的函数调用当前对象，有些函数支持第二个参数来修改当前函数中的this\n\n箭头函数的this在运行时绑定到执行函数的上下文\n\nfunc.call(context,...args)设置函数的this为context，并调用函数\n\nfunc.apply(context,args)设置函数的this为context，并调用函数，args为类数组对象\n\nfunc.bind(context,arg1,arg2,...)设置函数的this为context，返回一个新的函数，arg的参数优先级大于调用新函数时参数的优先级\n\n\n","categories":["笔记"],"tags":["JavaScript"]},{"title":"Window下Hexo建站","url":"https://baizuige.github.io/2021/01/23/Window下Hexo建站/","content":"建站准备\n安装git\n安装node.js，长期支持版即可\n\n实操部分安装完git后鼠标右键可以看到Git Bash Here操作，打开后可以使用一些linux的命令；选择自己想要存放博客文件的文件夹内，执行该操作，之后的操作都在这里执行。\n\n安装并切换阿里镜像\n# npm install cnpm -g --registry&#x3D;https:&#x2F;&#x2F;registry.npm.taobao.org\n# npm config set registry https:&#x2F;&#x2F;registry.npm.taobao.org\n安装hexo\n# npm install hexo-cli -g\n初始化一个hexo博客\n# hexo init\n此时博客已经成功生成，可以使用hexo s在本地预览\n\n\n部署远端这里推荐部署在github上，免费且方便；也可以部署在gitee上，同样是免费的还是在git命令行中执行以下操作\n\nGit初始化配置\n# git config --global user.name &quot;填写自己github的用户名&quot;\n# git config --global user.email 填写绑定的邮箱号\n进入github，创建一个以”自己用户名.github.io”为名称的仓库\n\n复制仓库地址，HTTPS和SSH地址都行\n\n配置hexo\n\n在博客文件夹中打开_config.yml文件\n\n翻到最后，输入以下代码，其中repo后输入刚才复制的地址\n# Deployment\n## Docs: https:&#x2F;&#x2F;hexo.io&#x2F;docs&#x2F;one-command-deployment\ndeploy:\n  type: git\n  repo: 仓库地址\n  branch: main\n\n\n回到git命令行，输入hexo d即可将博客部署到远端；部署时可能会出现错误ERROR Deployer not found: git 解决办法是安装hexo-deployer-git插件\n# npm install --save hexo-deployer-git\n回到github仓库，在设置中找到GitHub Pages，将Source中的Branch改为main后保存(save)，之后就可以在”自己用户名.github.io”的网址下访问博客\n\n\n常用命令\nhexo new &quot;文章名称&quot; 在\\source\\_posts目录下创建一篇文章，markdown格式书写，推荐使用typora\nhexo g 生成静态文件\nhexo s 本地预览\nhexo d 推送远端、hexo d -g 部署之前预先生成静态文件\nhexo clean 清除缓存文件和已生成的静态文件，更换主题后可能用到\n\n","categories":["笔记"],"tags":["Hexo","Window"]}]